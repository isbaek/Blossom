import React from 'react-native'

export default React.StyleSheet.create({
  EventTypeIcon: {
    backgroundColor: "#fff",
    color: "#333",
    borderRadius: 15,
    padding: 7,
    fontSize: 16,
    width: 30,
    height: 30,
    textAlign: "center",
    overflow: 'hidden',
  },
})
